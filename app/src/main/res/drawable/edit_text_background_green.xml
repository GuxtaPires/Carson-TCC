<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Fundo branco -->
    <solid android:color="#FFFFFF" />

    <!-- Bordinha verde super fina -->
    <stroke
        android:width="1dp"
        android:color="#6ED26E" />

    <!-- Arredondamento estilo pílula -->
    <corners android:radius="50dp" />

    <!-- Padding interno suave -->
    <padding
        android:left="12dp"
        android:top="8dp"
        android:right="12dp"
        android:bottom="8dp" />
</shape>
